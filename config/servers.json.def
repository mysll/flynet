{
	"master": {
		"#host": "master的IP地址,如果agent设为true,则为主master的地址",
		"host": "127.0.0.1",
		"#port": "主master的监听端口,agent需要和主master保持一致",
		"port": 5100,
		"#localip": "本机内网IP地址,会提供给当前机器上服务使用",
		"localip": "127.0.0.1",
		"#outerip": "本机外网IP地址",
		"outerip":"192.168.1.102",
		"#consoleport": "控制台端口",
		"consoleport": 8700,
		"#consoleroot": "控制台根目录",
		"consoleroot": "../console",
		"#agent": "是否启动为代理模式",
		"agent": true,
		"#agentid": "代理服务器ID",
		"agentid": "agent_1",
		"#waitagents": "主服务器需要多少个代理服务器，如果这个值大于0，则主服务器会等待代理服务器的接入，满足要求后，才会启动整个服务",
		"waitagents": 1,
		"#startuid": "当前服务器的服务起始ID段，用来产生唯一的服务ID",
		"startuid": 1000,
		"#nobalance": "不参与负载均衡，主服务器不会在当前服务器中启动其它服务，用来做独占服务，比如数据库服务。",
		"nobalance":true
	},
	"login": [{
		"#name": "名称,全局唯一，不能重复",
		"name": "login_1",
		"#port": "端口，为0，则为随机端口",
		"port": 0,
		"#clientport": "供客户端连接的端口，为0，则为随机端口",
		"clientport": 0,
		"#fronted": "是否提供客户端服务，为true,打开clientport端口",
		"fronted": true,
		"#sockettype": "socket类型, 可选websocket, native, 默认为native",
		"sockettype": "native"
	}],
	"loginmgr": [{
		"#name": "名称,全局唯一，不能重复",
		"name": "loginmgr",
		"#port": "端口，为0，则为随机端口",
		"port": 0,
		"#clientport": "供客户端连接的端口，为0，则为随机端口",
		"clientport": 5391,
		"#fronted": "是否提供客户端服务，为true,打开clientport端口",
		"fronted": true,
		"#sockettype": "socket类型, 可选websocket, native, 默认为native",
		"sockettype": "native"
	}],
	"base": [{
		"#name": "名称,全局唯一，不能重复",
		"name": "base_1",
		"#port": "端口，为0，则为随机端口",
		"port": 0,
		"#clientport": "供客户端连接的端口，为0，则为随机端口",
		"clientport": 0,
		"#fronted": "是否提供客户端服务，为true,打开clientport端口",
		"fronted": true,
		"#sockettype": "socket类型, 可选websocket, native, 默认为native",
		"sockettype": "native",
		"#assets": "资源目录",
		"assets":"../assets",
		"#loglevel":"日志记录等级，0:FINEST，1:FINE，2:DEBUG，3:TRACE，4:INFO，5:WARNING，6:ERROR，7:CRITICAL",
		"loglevel":2,
		"#debug": "是否打开pprof",
		"debug":true,
		"#pprof": "pprof监听端口",
		"pprof":6800
	}],
	"basemgr": [{
		"#name": "名称,全局唯一，不能重复",
		"name": "basemgr",
		"#port": "端口，为0，则为随机端口",
		"port": 0
	}],
	"status": [{
		"#name": "名称,全局唯一，不能重复",
		"name": "status",
		"#port": "端口，为0，则为随机端口",
		"port": 0,
		"#globalset":"全局数据的entity名称，默认为GlobalSet",
		"globalset":"GlobalSet",
		"#enableglobaldata":"是否开启全局数据,默认不开启",
		"enableglobaldata":false
	}],
	"database": [{
		"#name": "名称,全局唯一，不能重复",
		"name": "db_1",
		"#port": "端口，为0，则为随机端口",
		"port": 0,
		"#type": "数据库类型，mysql, mongodb",
		"type": "mysql",
		"#datasource": "数据库连接串",
		"datasource": "root:@tcp(192.168.1.10:3306)/mmosll?charset=utf8",
		"#role": "角色对应的entity类型",
		"role": "Player",
		"#db": "使用的数据库名称",
		"db": "mmosll",
		"#entity": "entity定义文件的路径",
		"entity": "../assets/def",
		"#pools": "数据库操作线程数",
		"pools": 4,
		"#rolelimit": "帐号角色数量上限",
		"rolelimit":1,
		"#rolenameunique": "帐号角色名是否唯一，0不唯一，1唯一",
		"rolenameunique":1
	}]
}